version: '3.8'

services:
  scanner:
    build: .
    container_name: pattern-scanner-alpaca
    ports:
      - "5004:5004"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data
